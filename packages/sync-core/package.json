{
  "name": "@pazarsync/sync-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test:unit": "PAZARSYNC_SKIP_RESEED=1 vitest run src --passWithNoTests",
    "test:integration": "vitest run tests/integration --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "clean": "rm -rf .turbo"
  },
  "dependencies": {
    "@pazarsync/crypto-core": "workspace:*",
    "@pazarsync/db": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "dotenv": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@pazarsync/eslint-config": "workspace:*",
    "eslint": "catalog:"
  }
}
