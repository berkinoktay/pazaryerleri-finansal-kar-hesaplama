// Hono backend (no React, no Next.js) -- uses the shared node/TypeScript flat
// config from @pazarsync/eslint-config. The dump-openapi.ts script and tests/
// are linted alongside src/.
import base from '@pazarsync/eslint-config';

export default base;
